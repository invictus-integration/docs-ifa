---
sidebar_label: Architecture
sidebar_position: 2
hide_table_of_contents: true
---

import { Flow, Flows, FlowTrace, FlowTraces, FlowTraceImporting } from '@site/src/components/TermHighlight';
import ComponentCard from '@site/src/components/ComponentCard';

# Invictus for Azure architecture
An Invictus installation consists of two major parts: the **Dashboard** and the **Framework**.

## Dashboard
The Invictus **Dashboard** provides a business user friendly UI for interacting with client integrations (commonly Azure Logic Apps), without requiring direct access to Azure. The Dashboard displays integrations as <Flows variant="technical"/>, supported by diagnostic data (here called <FlowTraces/>) loaded via Invictus' backend infrastructure.

<br/>
![Architecture diagram](/images/InvictusV2Diagram_Dashboard.png "Invictus Dashboard architecture diagram")
<br/>

* <Flow variant="technical" />
  * <u>**Dashboard App:**</u> the business user interacts with the **Dashboard** via a deployed web app. Here, users manage their flows.
  * <u>**Dashboard Gateway:**</u> serves as the backend API for the **Dashboard App**. It shows the stored flows (*Cosmos DB for MongoDB*) and triggers flow operations (*Azure Service Bus*).  
  * <u>**Flow Handler:**</u> based on the required flow action triggered by the business user (*Ignore*, *Resubmit*, *Resume*) in the **Dashboard App**, it interacts with the deployed Azure Logic Apps on the client environment (See [import flow traces via Logic App workflows](./dashboard/flows/04_import-flow-traces/import-flows-via-la.mdx) for more info).
* <FlowTrace />
  * <u>**[type] import job:**</u> the Invictus installation provides 'import jobs' which are interaction endpoints to push flow traces into the **Dashboard**. These come from external client resources (Azure Logic Apps, Azure Data Factory, Azure Event Hubs). Once received, they push a canonical message into the system (*Azure Service Bus*).
  * <u>**Cache job:**</u> listens for new canonical messages (*Azure Service Bus*) and caches them as batches (*Azure Blob Storage*).
  * <u>**Merge job:**</u> listens for new/updated batches (*Azure Event Hubs*) and synchronizes the storage (*Cosmos DB for MongoDB*) with 'message content view' information (input/output of Azure Logic Apps, see [import flow traces via Azure Logic App workflows](./dashboard/flows/04_import-flow-traces/import-flows-via-la.mdx) for more info).
  * <u>**Store job:**</u> listens for new batches (*Azure Event Hubs*), loads referenced canonical message (*Azure Blob Storage*), and combines as flow trace models (*Cosmos DB for MongoDB*). These flow traces get available via the **Dashboard App** to the business user.

## Framework
The Invictus **Framework** provides a set of reusable components to support Azure Logic Apps workflows or other integrations.

<Columns>
  <Column style={{marginBottom: "1.5rem"}}>
    <ComponentCard
      icon="/img/icons/transco.png"
      name="Transco"
      tagline="Property promotion & transformation"
      description="Promote client properties from external/internal data sources and perform XSLT transformations."
      href="./framework/transcoV2"
    />
  </Column>
  <Column style={{marginBottom: "1.5rem"}}>
    <ComponentCard
      icon="/img/icons/pubsub.png"
      name="PubSub"
      tagline="Publish / subscribe messaging"
      description="Decouples Logic App workflows into publishers and subscribers using Azure Service Bus, enabling reliable cross-workflow messaging."
      href="./framework/pubsubV2"
    />
  </Column>
  <Column style={{marginBottom: "1.5rem"}}>
    <ComponentCard
      icon="/img/icons/exception-handler.png"
      name="Exception Handler"
      tagline="Failure diagnostics"
      description="Captures Logic App workflow failures and explains error diagnostics, giving operators clear insight into what went wrong and where."
      href="./framework/exceptionHandler"
    />
  </Column>
</Columns>

<Columns>
  <Column style={{marginBottom: "1.5rem"}}>
    <ComponentCard
      icon="/img/icons/sequence-controller.png"
      name="Sequence Controller"
      tagline="Index-based ordering"
      description="Orchestrates Logic App workflow execution order based on custom sequence indices, ensuring strict processing order regardless of trigger times."
      href="./framework/sequencecontroller"
    />
  </Column>
  <Column style={{marginBottom: "1.5rem"}}>
    <ComponentCard
      icon="/img/icons/time-sequencer.png"
      name="Time Sequencer"
      tagline="Timestamp-based ordering"
      description="Orchestrates Logic App workflow execution order based on custom timestamps, ensuring time-controlled processing order regardless of trigger times."
      href="./framework/timesequencer"
    />
  </Column>
  <Column style={{marginBottom: "1.5rem"}}>
    <ComponentCard
      icon="/img/icons/regex-translator.png"
      name="Regex Translator"
      tagline="Pattern-based translation"
      description="Matches and replaces values in message content using configurable regular expression patterns."
      href="./framework/regextranslation"
    />
  </Column>
</Columns>

<Columns>
  <Column style={{marginBottom: "1.5rem"}}>
    <ComponentCard
      icon="/img/icons/xml-json-converter.png"
      name="XML/JSON Converter"
      tagline="Format conversion"
      description="Transforms XML payloads to JSON or JSON payloads to XML using a custom XSLT stylesheet."
      href="./framework/xmljsonconverter"
    />
  </Column>
  <Column style={{marginBottom: "1.5rem"}}>
    <ComponentCard
      icon="/img/icons/xsd-validator.png"
      name="XSD Validator"
      tagline="XML schema validation"
      description="Validates XML payloads against an XSD schema definition, returning a structured validation result."
      href="./framework/xsd-validator"
    />
  </Column>
  <Column style={{marginBottom: "1.5rem"}} />
</Columns>