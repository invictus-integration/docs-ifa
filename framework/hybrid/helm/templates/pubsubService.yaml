apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.PubSub.OpenApiInfoTitle |lower | nospace}}-service
spec:
  selector:
     function: {{ .Values.PubSub.OpenApiInfoTitle |lower | nospace}}
  ports:
  - name: pubsub
    protocol: TCP
    port: {{ .Values.port }}
    targetPort: {{ .Values.port }}
  - name: http
    protocol: TCP
    port: 80
    targetPort: 80