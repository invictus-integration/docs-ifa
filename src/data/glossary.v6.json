[
  {
    "term": "Flow",
    "definition": "A named message chain — a group of **messages** that share one origin. [Flows sit in *folders*](/dashboard/flows#organizing-flows-in-folders) and have a name, mappings, and properties.",
    "userType": "business"
  },
  {
    "term": "Flow",
    "definition": "A named message chain — a logical grouping of related **flow traces** sharing a common origin. [Flows are organized in *folders*](/dashboard/flows#organizing-flows-in-folders) and defined by a name, optional mappings, and business properties.",
    "userType": "technical"
  },
  {
    "term": "Flow trace",
    "definition": "A single tracked message within a **flow**. It records the full run: status, timestamps, properties, and each step.\n\nEach flow trace can be searched and viewed in the Dashboard.",
    "userType": "business"
  },
  {
    "term": "Flow trace",
    "definition": "A single tracked message instance within a **flow**. It records the full lifecycle of one execution: status, timestamps, properties, and the chain of processing steps.\n\nEach flow trace can be searched and inspected in the Dashboard, and is loaded via **flow trace importing**.",
    "userType": "technical"
  },
  {
    "term": "Flow trace importing",
    "aliases": [
      "import job"
    ],
    "definition": "The process of ingesting **flow trace** data into the Dashboard — automatically via Logic Apps or Function Apps, or manually. Once imported, messages become visible and searchable in the Dashboard",
    "userType": "technical"
  },
  {
    "term": "Invictus Framework",
    "aliases": [
      "Framework"
    ],
    "definition": "A set of building blocks for common integration patterns in Azure.",
    "userType": "business"
  },
  {
    "term": "Invictus Dashboard",
    "aliases": [
      "Dashboard"
    ],
    "definition": "A tool that lets business users track their integrations, without deep Azure knowledge.",
    "userType": "business"
  },
  {
    "term": "DSAV",
    "definition": "Stands for **Domain**, **Service**, **Action**, and **Version** — four labels Invictus uses to group messages. In the Dashboard, you can filter by any of these to find similar messages.",
    "userType": "business"
  },
  {
    "term": "Chain ID",
    "definition": "An ID that groups every **flow trace** in one process. In the Dashboard, use Chain ID to see every message in that process, across all flows.",
    "userType": "business"
  },
  {
    "term": "Flow status",
    "definition": "The state of a **flow trace** in the Dashboard. Each flow has one of five states.\n\n- **Active** — the flow is running now.\n\n- **Completed** — the flow finished with no errors.\n\n- **Error** — the flow stopped due to a failure.\n\n- **Ignored** — a user closed it after reviewing the failure.\n\n- **Cancelled** — a user stopped the flow before it could finish.",
    "userType": "business"
  },
  {
    "term": "Resubmit",
    "definition": "A Dashboard action that re-runs a **flow trace** from the start. Resubmit runs the whole chain again, even if it worked the first time.\n\nSee also **Resume**, which only re-runs the failed steps, not the whole chain.",
    "userType": "business"
  },
  {
    "term": "Resume",
    "definition": "A Dashboard action that re-runs only the failed steps of a **flow trace**, without starting the whole chain over. Resume only works for messages in **Error** status.\n\nSee also **Resubmit**, which always restarts from the first workflow.",
    "userType": "business"
  },
  {
    "term": "Folder",
    "definition": "A folder groups **flows** together in the Dashboard menu. You can nest folders to give teams a clear view of their integrations.",
    "userType": "business"
  },
  {
    "term": "DSAV",
    "definition": "Stands for **Domain**, **Service**, **Action**, and **Version** — four special **tracked properties** that Invictus reads during **flow trace importing** to map workflow runs to the correct Dashboard flow.\n\nThese values must match the corresponding properties configured on the flow in the Dashboard. Invictus also reads a `WorkflowName` field alongside DSAV to resolve the exact flow mapping.",
    "userType": "technical"
  },
  {
    "term": "Chain ID",
    "aliases": [
      "clientTrackingId",
      "client tracking ID"
    ],
    "definition": "The Invictus name for the Azure Logic Apps `clientTrackingId`. It groups all **flow traces** belonging to the same transaction.\n\nBecause async flows may regenerate this ID during routing, developers must reset it as early as possible in each workflow to keep the chain intact.",
    "userType": "technical"
  },
  {
    "term": "Tracked properties",
    "definition": "Custom metadata fields set on Azure Logic App actions that Invictus reads when importing **flow traces**. They drive the key Dashboard features:\n\n- **DSAV** and `WorkflowName` — map workflow runs to the correct flow.\n- `x-iv-parent-workflow-run-id` — links child workflows to their parent to build the **execution tree**.\n- `Milestone` / `EventText` — annotate processing steps shown in the flow detail view.\n- `x-iv-messagecontent-*` — expose input/output content of specific actions in the Dashboard.",
    "userType": "technical"
  },
  {
    "term": "Execution tree",
    "definition": "The hierarchical view of parent and child runs linked within a single **flow trace**. Invictus builds this tree from the `x-iv-parent-workflow-run-id` **tracked property** set by developers when chaining workflows.\n\nThe execution tree is visible in the Dashboard flow detail view and shows the full processing chain for a message.",
    "userType": "technical"
  },
  {
    "term": "Exception Handler",
    "definition": "An **Invictus Framework** component that resolves the actual error from a failed Azure Logic App workflow run. It exposes a `/api/ResolveException` endpoint that retrieves the underlying error message — replacing the generic Azure error — and can optionally translate it to human-readable text via a configurable regex translation table.",
    "userType": "technical"
  },
  {
    "term": "PubSub",
    "definition": "An **Invictus Framework** component that enables publish/subscribe messaging between Azure Logic App workflows. It uses Azure Service Bus as a message broker. For messages exceeding a size limit, PubSub offloads the body to Azure Blob Storage. See also **claim-check pattern**.",
    "userType": "technical"
  },
  {
    "term": "Sequence Controller",
    "definition": "An **Invictus Framework** component that runs Azure Logic App workflows in a fixed order. It does not use concurrency throttling to enforce this.\n\nEach workflow claims a spot in a named sequence. It waits its turn, does its work, then signals the next one to start.\n\nThis keeps workflows in order even when they start at the same time. It does not block the whole Logic App.",
    "userType": "technical"
  },
  {
    "term": "Transco",
    "definition": "An **Invictus Framework** component (originating from a BizTalk pattern) that enriches messages during processing. It can:\n\n- **Promote** values from an SQL database into the message or context, using the message content or context to build the query.\n- **Transform** XML content using an XSLT stylesheet stored in Azure Blob Storage.",
    "userType": "technical"
  },
  {
    "term": "Claim-check pattern",
    "aliases": [
      "claim check"
    ],
    "definition": "An **Invictus Framework** behavior used in **PubSub**. When a published message has a body that exceeds a size limit (default 20,000 bytes), the Publish action offloads the body in Azure Blob Storage instead and adds a reference in the message.\n\nThe Subscribe action gets the body from storage by the reference.\n\n*See also the [claim-check integration pattern](https://learn.microsoft.com/en-us/azure/architecture/patterns/claim-check).*",
    "userType": "technical"
  }
]